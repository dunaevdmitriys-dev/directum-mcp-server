---
id: ds_sozd_funkciy_vyrazheniy
module: ds
role: Developer
topic: Создание функций вычисляемых выражений
breadcrumb: "Разработка > Программный код > Функции"
description: "Функции используются для преобразования данных при настройке вычисляемых выражений. Созданные функции отображаются в выпадающем списке с элементами выражения. При создании..."
source: webhelp/WebClient/ru-RU/ds_sozd_funkciy_vyrazheniy.htm
---

# Создание функций вычисляемых выражений

Функции используются для преобразования данных при настройке вычисляемых выражений . Созданные функции отображаются в выпадающем списке с элементами выражения. При создании функций учитывайте их особенности и рекомендации по именованию .

## Особенности создания функций

Функции вычисляемых выражений:

- создаются в серверном коде с использованием атрибута ExpressionElement ;
- должны быть статичными и публичными, а также возвращать результат;
- могут содержать любое число параметров. При этом:

• первый параметр функции определяет, для какого контекста эта функция может быть использована. В данном случае, контекст – тип свойства или результата другой функции, следом за которой может быть выбрана текущая функция;

• при вызове функции первый параметр заполняется автоматически. Поэтому, если в коде функции есть только один параметр, то в вычисляемом выражении она отображается без параметров. Если в коде задано два параметра, то в вычисляемом выражении отображается только второй;

• функции могут быть без параметров, то есть вызываться без контекста. С таких функций может начинаться вычисляемое выражение, а также они могут быть начальными для параметров функций.

Вычисляемые функции рекомендуется создавать в серверных функциях:

- модуля, если они общие и не привязаны к какой-то конкретной сущности;
- типа сущности, если они используются только для конкретного типа сущности.

## Порядок создания функции

Предположим, необходимо написать функцию, которая возвращает руководителя инициатора задачи. Для этого:

- 1. Создайте серверную функцию модуля.
- ПРИМЕЧАНИЕ. Функцию рекомендуется создавать в модуле, так как в данном случае она не привязана к определенному типу сущности.
- 2. В редакторе кода укажите нужное имя функции, добавьте комментарий и напишите код. Перед объявлением функции укажите атрибут ExpressionElement :

```csharp
/// <summary>
/// Получить руководителя пользователя, который отправил задачу.
/// </summary>
/// <param name="task">Задача.</param>
/// <returns>Руководитель пользователя, который отправил задачу </returns>
[ExpressionElement("Руководитель пользователя", "Возвращает CEO пользователя")]
public static Sungero.CoreEntities.IRecipient СEOofAuthor(Sungero.Workflow.ITask task)
{
return Sungero.Company.Employees.Get(task.Author.Id).Department.BusinessUnit.CEO;
}
```

- 3. Сохраните изменения.
- 4. Опубликуйте разработку .

В результате созданную функцию можно выбрать при создании вычисляемых выражений.

ВАЖНО. Если функцию нужно удалить, то в вычисляемых выражениях ее необходимо заменить на другую, иначе при получении значения возникнет ошибка.

Если функция должна быть доступна только в выражении для конкретного типа документа, то ее рекомендуется создавать в серверных функциях этого типа. Например, чтобы получить сумму договора, используйте код:

```csharp
/// </summary>
/// Получить сумму договора.
/// </summary>
/// <param name="doc">Договор.</param>
/// <returns> Сумма </returns>
[ExpressionElement("Получить сумму")]
public static double? GetSum(IContract doc)
{
return doc.TotalAmount;
}
```

## Локализация функции

Название и описание функции, а также описания ее параметров можно локализовать. Для этого:

- 1. Добавьте ресурс для функции.
- 2. Добавьте перед функцией атрибут ExpressionElement и заполните его параметры.
- В отличие от других элементов разработки, в атрибуте используется только имя ресурса, а не полный путь. Поэтому замените его на имя ресурса в двойных кавычках:

```csharp
[ExpressionElement("Ceo_of_author", "Ceo_of_author_description")]
public static Sungero.CoreEntities.IRecipient СEOofAuthor(Sungero.Workflow.ITask task)
{
return Sungero.Company.Employees.Get(task.Author.Id).Department.BusinessUnit.CEO;
}
```

В результате после публикации наименование и описание функции отображаются в выражениях с учетом выбранной локализации.
