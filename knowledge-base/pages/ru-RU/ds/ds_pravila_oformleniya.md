---
id: ds_pravila_oformleniya
module: ds
role: Developer
topic: Правила оформления
breadcrumb: "Разработка > Программный код"
description: "Общие правила по оформлению программного кода: имена элементов разработки, свойств и действий задавайте как можно короче. При этом они должны быть осмысленными и говорящими,..."
source: webhelp/WebClient/ru-RU/ds_pravila_oformleniya.htm
---

# Правила оформления

Общие правила по оформлению программного кода:

- имена элементов разработки, свойств и действий задавайте как можно короче. При этом они должны быть осмысленными и говорящими, так как это упрощает читаемость кода и его понимание другими разработчиками;
- для идентификаторов переменных, параметров используйте нотацию camelCasing: первое слово пишется с маленькой буквы, остальные слова – с большой. Для всех остальных – PascalCasing: все слова пишутся слитно с большой буквы. Это нотация также используется для задания имен свойствам сущности. Подробнее см. в документации Microsoft статью «Соглашения о написании прописными буквами» ;
- сокращения в именах идентификаторов не должны влиять на понятность имени. Не используйте аббревиатуры или неполные слова в идентификаторах, если только они не являются общепринятыми;
- имя сущности задавайте в единственном числе. Так как в среде разработки автоматически генерируется пространство имен репозитория с именем сущности во множественном числе;
- избегайте лишних скобок, обрамляющих выражения целиком;
- содержимое класса или функции можно сдвинуть на нужное количество пробелов относительно объявления в зависимости от правил разработки в вашей организации:

```csharp
partial class ContractFunctions
{
/// <summary>
/// Показывать сводку по документу в заданиях на согласование и подписание.
/// </summary>
/// <returns>True, если в заданиях нужно показывать сводку по документу.</returns>
[Public]
public override bool NeedViewDocumentSummary()
{
return true;
}
...
}
```

## Настройка размера отступа

1. В корневой папке репозиториев или в каждой папке репозитория на рабочем слое создайте файл .editorconfig. 2. Добавьте содержимое файла: root = true [*.cs] indent_style = space indent_size = 2 3. Сохраните файл .

- переменные рекомендуется объявлять с помощью ключевого слова var вместо явного указания типа:

```csharp
/// <summary>
/// Проверить, что текущий пользователь входит в указанную роль.
/// </summary>
/// <param name="roleName">Имя роли.</param>
/// <returns>True, если входит в роль. Иначе - false.</returns>
public static bool IncludedInRole(string roleName)
{
var role = Roles.GetAll(r => r.Name == roleName).FirstOrDefault();
if (role == null)
{
return false;
}
var employee = Users.Current;
return employee.IncludedIn(role);
}
```

- Тип допускается указывать явно, когда это необходимо для корректной сборки и выполнения кода.
- пишите комментарии к коду в XML-формате.
- ПОДРОБНЕЕ

```csharp
/// <summary>
///
/// </summary>
/// <param name=""></param>
/// <returns></returns>
```
